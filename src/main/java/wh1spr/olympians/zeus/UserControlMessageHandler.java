package wh1spr.olympians.zeus;

import net.dv8tion.jda.core.hooks.ListenerAdapter;

/**
 * This MessageHandler takes care of bans, kicks, mutes,... which are timed events.
 * So, tempbans, timed mutes,...
 * There will be several roles placed on a high level that make this easier.
 * 
 * @author Robbe
 */
public class UserControlMessageHandler extends ListenerAdapter{


}
